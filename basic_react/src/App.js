function App() {
  return (
    <h1>This is React using Basic React Bundle</h1> // so in this line we are giving a H1 tag with something in it. It is then returned and fetched in the index.js fie.
  );
}

export default App;