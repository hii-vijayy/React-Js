import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

//this is a password genertator which generates password of default lengtha and also the length selected byt the user along with the feature like number and character included or not. It uses React hooks like useState and useCallback.

ReactDOM.createRoot(document.getElementById('root')).render
(
    <App />
)
